#!/bin/bash

# ============================================================
# 小智 ESP32 服务器重启脚本
# ============================================================

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"

echo "正在停止服务..."
"$SCRIPT_DIR/stop.sh"

sleep 2

echo "正在启动服务..."
"$SCRIPT_DIR/start.sh"

